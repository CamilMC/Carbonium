---
title: "Extraction données bassins versants"
author: "CamilMC"
date: "2026-07-02"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r libraries}
library(terra)
library(ncdf4)
library(sf)
library(exactextractr)
library(dplyr)
library(tidyr)
library(happign)
library(reticulate)
library(readxl)

library(ggplot2)
```

# Bassins versants

```{r load-bassins}
bassins <- st_read("BV/BV_WP1_v1.shp")   # ou .shp

info <- read_xlsx("../Etat_des_lieux_260506.xlsx", sheet = "Export_bv") %>% unique()

parse_annees <- function(chaine) {
  # Séparer sur la virgule
  elements <- strsplit(chaine, ",")[[1]]
  elements <- trimws(elements)  # enlever espaces éventuels
  
  # Pour chaque élément, dérouler si ":" présent, sinon garder tel quel
  vecteur <- unlist(lapply(elements, function(e) {
    if (grepl(":", e)) {
      bornes <- as.numeric(strsplit(e, ":")[[1]])
      seq(bornes[1], bornes[2])
    } else {
      as.numeric(e)
    }
  }))
  
  return(vecteur)
}


info$years <- lapply(info$years, parse_annees)

```
```{r crs-territoires}
library(sf); library(terra); library(exactextractr); library(dplyr); library(happign); library(reticulate)

# CRS légaux par territoire (nécessaires pour happign/RGE ALTI)
territoires <- tibble::tribble(
  ~territoire,   ~epsg, ~lon_min, ~lon_max, ~lat_min, ~lat_max,
  "metropole",    2154,     -5.5,     10.0,     41.0,     51.5,
  "guadeloupe",   5490,    -61.9,    -60.9,     15.8,     16.6,
  "martinique",   5490,    -61.3,    -60.7,     14.3,     15.0,
  "guyane",       2972,    -54.6,    -51.5,      2.0,      5.9,
  "reunion",      2975,     55.1,     55.9,    -21.5,    -20.8,
  "mayotte",      4471,     45.0,     45.4,    -13.1,    -12.5
)

# Fonction d'assignation par centroïde (WGS84)
assigner_territoire <- function(centroids_wgs84) {
  lon <- st_coordinates(centroids_wgs84)[,1]
  lat <- st_coordinates(centroids_wgs84)[,2]
  terr <- rep(NA_character_, length(lon))
  for (i in seq_len(nrow(territoires))) {
    t <- territoires[i, ]
    idx <- lon >= t$lon_min & lon <= t$lon_max & lat >= t$lat_min & lat <= t$lat_max
    terr[idx] <- t$territoire
  }
  terr[is.na(terr)] <- "hors_France"
  terr
}


```

```{r bassins}

bassins_wgs84 <- st_transform(bassins, 4326)
bassins_wgs84_valid <- st_make_valid(bassins_wgs84)
bassins_wgs84_centroid <- st_centroid(bassins_wgs84_valid)
bassins_wgs84_valid$territoire <- assigner_territoire(bassins_wgs84_centroid)

bassins_france <- filter(bassins_wgs84_valid, territoire == "metropole")
epsg_france <- 2154
bassins_france_proj <- st_transform(bassins_france, epsg_france)

bassins_guadeloupe <- filter(bassins_wgs84_valid, territoire == "guadeloupe")
epsg_guadeloupe <- 5490
bassins_guadeloupe_proj <- st_transform(bassins_guadeloupe, epsg_guadeloupe)

bassins_hors_france <- filter(bassins_wgs84_valid, territoire == "hors_France")
bassins_hors_france_proj <- bassins_hors_france %>% st_transform



table(bassins_wgs84_valid$territoire)  # vérifier la répartition
```
```{r check-epsg-cameroun-tunisie}
bbox <- st_bbox(bassins_hors_france[3,])
lon_moyenne <- (bbox["xmin"] + bbox["xmax"]) / 2
zone_utm <- floor((lon_moyenne + 180) / 6) + 1
zone_utm


```

# ERA 5 

Données téléchargées depuis https://cds.climate.copernicus.eu/datasets/reanalysis-era5-single-levels-monthly-means?tab=overview

A comparer avec données pluvio Isère / Galabre et moyennes de BV Fabien Seve.

## Precip


```{r data-precip, eval = F}
# --- 1. Charger le NetCDF ERA5 ---
sds(rast("ERA5/aec7517ff65614f8b538ee000cfe3eb2/data_stream-moda_stepType-avgad.nc"))  # liste les variables disponibles

# Charger la variable de précipitation
r <- rast("ERA5/aec7517ff65614f8b538ee000cfe3eb2/data_stream-moda_stepType-avgad.nc", subds = "tp")

library(ncdf4)
nc <- nc_open("ERA5/aec7517ff65614f8b538ee000cfe3eb2/data_stream-moda_stepType-avgad.nc")
vt <- ncvar_get(nc, "valid_time")
dates <- as.POSIXct(vt, origin = "1970-01-01", tz = "UTC")
nc_close(nc)


# Associer les dates du ncdf au raster
time(r) <- dates
names(r) <- format(dates, "%Y-%m-%d") 


# Transform le crs des bassins pour l'adapter au raster

bassins <- st_transform(bassins, crs(r))       # aligner les CRS

bassins_shifted <- st_shift_longitude(bassins)

bassins_vect <- vect(bassins_shifted)

# Vérifier que l'étendue coïncide (ERA5 est parfois en longitudes 0-360)

ext(bassins_vect)
ext(r)

 # Extract

result <- terra::extract(r, bassins_shifted, fun = mean, na.rm = TRUE) # moyenne sur le polygone, 1 valeur par mois
result$Name <- bassins_shifted$Name
result$Station <- bassins_shifted$Station
```


```{r mean-precip, eval = F}
all_precip <- merge(result, info, by = c("Name","Station"))


# fonction pour calculer la moyenne sur les années concernées
all_precip$mean_selected <- lapply(seq(nrow(all_precip)), function(i){
  years_i <- all_precip$years[[i]]
  cols_to_use <- lapply(years_i, grep,colnames(all_precip)) %>% unlist()
  mean_precip <- rowMeans(all_precip[i,cols_to_use], na.rm = T)
  return(mean_precip)
}) %>% unlist()

all_precip$mean_30y <- lapply(seq(nrow(all_precip)), function(i){
  years_i <- c(max(all_precip$years[[i]])-30, max(all_precip$years[[i]]))
  cols_to_use <- lapply(years_i, grep,colnames(all_precip)) %>% unlist()
  mean_precip <- rowMeans(all_precip[i,cols_to_use], na.rm = T)
  return(mean_precip)
}) %>% unlist()

bassins_precip <- all_precip %>% select(c("Name","Station","Obs","years","mean_selected","mean_30y"))
bassins_precip$year_vec <- bassins_precip$years %>% paste()
bassins_precip$years <- NULL

write.csv(bassins_precip, "ERA5/bassins_precip.csv")
```


```{r mean-precip-plot}
bassins_precip <- read.csv("ERA5/bassins_precip.csv")

ggplot(bassins_precip, aes(x=mean_selected,y = mean_30y))+
  geom_label(aes(label = Name, col = Name), size = 2)+
  geom_abline(slope = 1, intercept = 0, linetype = 2) + theme_bw()+
  theme(legend.position = "none")
```


Regarder le nombre de jours de pluie ? Définies par Gérémy et Nathan comme jours avec > 1mm de pluie (il faudrait prendre la réanalyse journalière et compter les jours)


## temp


```{r data-temp, eval = F}
# --- 1. Charger le NetCDF ERA5 ---
sds(rast("ERA5/aec7517ff65614f8b538ee000cfe3eb2/data_stream-moda_stepType-avgua.nc"))  # liste les variables disponibles

# Charger la variable de précipitation
r_temp <- rast("ERA5/aec7517ff65614f8b538ee000cfe3eb2/data_stream-moda_stepType-avgua.nc", subds = "t2m")


nc_temp <- nc_open("ERA5/aec7517ff65614f8b538ee000cfe3eb2/data_stream-moda_stepType-avgad.nc")
vt_temp <- ncvar_get(nc_temp, "valid_time")
dates_temp <- as.POSIXct(vt_temp, origin = "1970-01-01", tz = "UTC")
nc_close(nc_temp)


# Associer les dates du ncdf au raster
time(r_temp) <- dates
names(r_temp) <- format(dates_temp, "%Y-%m-%d") 


# Transform le crs des bassins pour l'adapter au raster

bassins_temp <- st_transform(bassins, crs(r_temp))       # aligner les CRS

bassins_temp_shifted <- st_shift_longitude(bassins_temp)

bassins_temp_vect <- vect(bassins_temp_shifted)

# Vérifier que l'étendue coïncide (ERA5 est parfois en longitudes 0-360)

ext(bassins_temp_vect)
ext(r_temp)

 # Extract

result_temp <- terra::extract(r_temp, bassins_temp_shifted, fun = mean, na.rm = TRUE) # moyenne sur le polygone, 1 valeur par mois
result_temp$Name <- bassins_temp_shifted$Name
result_temp$Station <- bassins_temp_shifted$Station
```


```{r mean-temp, eval = F}
all_temp <- merge(result_temp, info, by = c("Name","Station"))


# fonction pour calculer la moyenne sur les années concernées
all_temp$mean_selected <- lapply(seq(nrow(all_temp)), function(i){
  years_i <- all_temp$years[[i]]
  cols_to_use <- lapply(years_i, grep,colnames(all_temp)) %>% unlist()
  mean_temp <- rowMeans(all_temp[i,cols_to_use], na.rm = T)
  return(mean_temp)
}) %>% unlist()

all_temp$mean_30y <- lapply(seq(nrow(all_temp)), function(i){
  years_i <- c(max(all_temp$years[[i]])-30, max(all_temp$years[[i]]))
  cols_to_use <- lapply(years_i, grep,colnames(all_temp)) %>% unlist()
  mean_temp <- rowMeans(all_temp[i,cols_to_use], na.rm = T)
  return(mean_temp)
}) %>% unlist()


bassins_temp <- all_temp %>% select(c("Name","Station","Obs","years","mean_selected","mean_30y"))
bassins_temp$year_vec <- bassins_temp$years %>% paste()
bassins_temp$years <- NULL

write.csv(bassins_temp, "ERA5/bassins_temp.csv")
```


```{r mean-temp, eval = F}
bassins_temp <- "ERA5/bassins_temp.csv"

ggplot(bassins_temp, aes(x=mean_selected,y = mean_30y))+
  geom_label(aes(label = Name, col = Name), size = 2)+
  geom_abline(slope = 1, intercept = 0, linetype = 2) + theme_bw() +
  theme(legend.position = "none")
```




# Code pour pente et altitude à partir de Copernicus GLO-30 et Happign

https://portal.opentopography.org/raster?opentopoID=OTSDEM.032021.4326.3

https://paul-carteron.github.io/happign/

https://gee-community-catalog.org/projects/fabdem/ GLO-30 sans les arbres

```{r fabdem, eval = F}
# Pour FABDEM (une seule fois)
reticulate::py_install("fabdem", pip = TRUE)

```


```{r extraction-pente-altitude, eval = F}
extraire_stats <- function(mnt, polygones, id_col = "nom_bassin") {
  pente <- terrain(mnt, v = "slope", unit = "degrees", neighbors = 8)
  tibble::tibble(
    !!id_col := polygones[[id_col]],
    altitude_moyenne = exact_extract(mnt,   polygones, "mean"),
    altitude_max      = exact_extract(mnt,   polygones, "max"),
    pente_moyenne     = exact_extract(pente, polygones, "mean"),
    pente_max         = exact_extract(pente, polygones, "max")
  )
}

calc_res_adaptee <- function(bbox, max_pixels = 4000) {
  largeur <- bbox["xmax"] - bbox["xmin"]
  hauteur <- bbox["ymax"] - bbox["ymin"]
  res_min <- max(largeur, hauteur) / max_pixels
  ceiling(res_min)   # arrondi au mètre supérieur
}

```

```{r traitement, eval = F}
layers_metadata <- get_layers_metadata("wms-r", "altimetrie")
dem_layer <- "ELEVATION.ELEVATIONGRIDCOVERAGE"
```

```{r essai-resolution-adaptee-france, eval = F}


### tout intégré
resultats_bassins <- list()

for (i in seq_len(nrow(bassins_france_proj))) {
  bassin_i <- bassins_france_proj[i, ]
  bbox_i <- st_bbox(bassin_i)
  res_i <- calc_res_adaptee(bbox_i, max_pixels = 3000)
  
  mnt_i <- get_wms_raster(
    x     = bassin_i,
    layer = dem_layer,
    res   = res_i,
    crs   = epsg_france,
    rgb   = FALSE
  )
  
  res <- extraire_stats(mnt_i, bassin_i) %>%
    mutate(resolution_utilisee = res_i, source = "IGN_RGE_ALTI")
  
  resultats_bassins[[i]] <- res
}

resultats_bassins <- bind_rows(resultats_bassins) %>% cbind(bassins_france[,c("Name", "Station","Obs")])

```

```{r resolution-adaptee-guadeloupe, eval = F}

### tout intégré
resultats_bassins_guadeloupe <- list()

for (i in seq_len(nrow(bassins_guadeloupe_proj))) {
  bassin_i <- bassins_guadeloupe_proj[i, ]
  bbox_i <- st_bbox(bassin_i)
  res_i <- calc_res_adaptee(bbox_i, max_pixels = 3000)
  
  mnt_i <- get_wms_raster(
    x     = bassin_i,
    layer = dem_layer,
    res   = res_i,
    crs   = epsg_guadeloupe,
    rgb   = FALSE
  )
  
  res <- extraire_stats(mnt_i, bassin_i) %>%
    mutate(resolution_utilisee = res_i, source = "IGN_RGE_ALTI")
  
  resultats_bassins_guadeloupe[[i]] <- res
}

resultats_bassins_guadeloupe <- bind_rows(resultats_bassins_guadeloupe) %>% cbind(bassins_guadeloupe[,c("Name", "Station","Obs")])

```

```{r essai-elevatr-bassins-hors-france, eval = F}
library(elevatr)
mnt_srtm <- get_elev_raster(bassins_hors_france, z = 12, src = "aws")  # SRTM/AW3D via AWS # 15 is maximum zoom

res_hors_france <- extraire_stats(mnt_srtm, bassins_hors_france) %>% mutate(resolution_utilisee = 30, source = "elevatr") %>% cbind(bassins_hors_france[,c("Name", "Station","Obs")])

```

```{r fusion, eval = F}
dem_wp1 <- bind_rows(resultats_bassins, resultats_bassins_guadeloupe,res_hors_france)
write.csv(dem_wp1, "DEM/dem_wp1.csv", row.names = FALSE)
```

# Pédologie

## Préparation des données 

```{r metadata-hwsd, eval = F}
sf::st_layers("Pédologie/HWSD2_DB/HWSD2.mdb")

smu_table <- st_read("Pédologie/HWSD2_DB/HWSD2.mdb", layer = "HWSD2_SMU")       # à ajuster selon le nom exact
symbol_table <- st_read("Pédologie/HWSD2_DB/HWSD2.mdb", layer = "D_FAO90")   # idem

# Convertir en data.frame classique (pas de géométrie dans ces tables attributaires)
smu_table <- as.data.frame(smu_table)


correspondance_fao <- smu_table %>%
  distinct(FAO90) %>%
  arrange(FAO90) %>%
  mutate(code_fao90 = row_number())

smu_table <- smu_table %>%
  left_join(correspondance_fao, by = "FAO90")
```


```{r raster-hwsd, eval = F}
hwsd <- rast("Pédologie/HWSD2_RASTER/HWSD2.bil")

m <- as.matrix(smu_table[, c("HWSD2_SMU_ID", "code_fao90")])
hwsd_fao <- classify(hwsd, m)
saveRDS(hwsd_fao, "Pédologie/hwsd_fao.rds")
```

## Extraction

```{r extraction, eval = F}

hwsd_fao <- readRDS("Pédologie/hwsd_fao.rds")

resultats_sol <- tibble::tibble(
  Name = bassins_wgs84_valid$Name,
  Station = bassins_wgs84_valid$Station,
  Obs = bassins_wgs84_valid$Obs,
  code_fao90_dominant = exact_extract(hwsd_fao, bassins_wgs84_valid, "majority")
)

resultats_sol <- resultats_sol %>%
  merge(correspondance_fao, by.x = "code_fao90_dominant", by.y = "code_fao90")
write.csv(resultats_sol, "Pédologie/resultat_sols.csv")
write_xlsx(resultats_sol, "Pédologie/resultat_sols.xlsx")
```

# Lithologie

```{r read-shp-lith}
LiMW <- st_read("Lithologie/LiMW_Carbonium.shp")

# Metropole
geol_france <- st_read("Lithologie/geol_france_metro.shp") %>% st_transform(st_crs(bassins_france_proj))
intersection_france <- st_intersection(bassins_france_proj, geol_france)

# 2. Calcul de la surface de chaque morceau d'intersection
intersection_france$surface_part <- st_area(intersection_france)

# 3. Pour chaque bassin, sommer la surface par classe, puis garder la classe majoritaire
litho_france_1 <- intersection_france %>%
  st_drop_geometry() %>%
  group_by(Name, Station, Obs, xx) %>%
  summarise(surface_totale = sum(surface_part), .groups = "drop") %>%
  group_by(Name, Station, Obs) %>%
  slice_max(surface_totale, n = 1, with_ties = FALSE) %>%
  ungroup() %>%
  select(Name, Station, Obs, classe_dominante = xx)

litho_france_2 <- intersection_france %>%
  st_drop_geometry() %>%
  group_by(Name, Station, Obs, xx) %>%
  summarise(surface_totale = sum(surface_part), .groups = "drop") %>%
  group_by(Name, Station, Obs) %>%
  slice_max(surface_totale, n = 2, with_ties = FALSE) %>%
  slice(2) %>%              # garde uniquement la 2e ligne (2e rang) de chaque groupe
  ungroup() %>%
  select(Name, Station, Obs, classe_2 = xx)

litho_france <- merge(litho_france_1, litho_france_2, by = c("Name","Station","Obs"), all.x =T)


# Guadeloupe
geol_guadeloupe <- st_read("Lithologie/geol_guadeloupe.shp") %>% st_transform(st_crs(bassins_guadeloupe_proj))
intersection_guadeloupe <- st_intersection(bassins_guadeloupe_proj, geol_guadeloupe)

# 2. Calcul de la surface de chaque morceau d'intersection
intersection_guadeloupe$surface_part <- st_area(intersection_guadeloupe)

# 3. Pour chaque bassin, sommer la surface par classe, puis garder la classe majoritaire
litho_guadeloupe_1 <- intersection_guadeloupe %>%
  st_drop_geometry() %>%
  group_by(Name, Station, Obs, xx) %>%
  summarise(surface_totale = sum(surface_part), .groups = "drop") %>%
  group_by(Name, Station, Obs) %>%
  slice_max(surface_totale, n = 1, with_ties = FALSE) %>%
  ungroup() %>%
  select(Name, Station, Obs, classe_dominante = xx)

litho_guadeloupe_2 <- intersection_guadeloupe %>%
  st_drop_geometry() %>%
  group_by(Name, Station, Obs, xx) %>%
  summarise(surface_totale = sum(surface_part), .groups = "drop") %>%
  group_by(Name, Station, Obs) %>%
  slice_max(surface_totale, n = 2, with_ties = FALSE) %>%
  slice(2) %>%              # garde uniquement la 2e ligne (2e rang) de chaque groupe
  ungroup() %>%
  select(Name, Station, Obs, classe_2 = xx)

litho_guadeloupe <- merge(litho_guadeloupe_1, litho_guadeloupe_2, by = c("Name","Station","Obs"), all.x = T)


# Cameroun
geol_cameroun <- st_read("Lithologie/geol_cameroun.shp") %>% st_transform(st_crs(bassins_hors_france_proj))
intersection_cameroun <- st_intersection(bassins_hors_france_proj, geol_cameroun)

# 2. Calcul de la surface de chaque morceau d'intersection
intersection_cameroun$surface_part <- st_area(intersection_cameroun)

# 3. Pour chaque bassin, sommer la surface par classe, puis garder la classe majoritaire
litho_cameroun_1 <- intersection_cameroun %>%
  st_drop_geometry() %>%
  group_by(Name, Station, Obs, xx) %>%
  summarise(surface_totale = sum(surface_part), .groups = "drop") %>%
  group_by(Name, Station, Obs) %>%
  slice_max(surface_totale, n = 1, with_ties = FALSE) %>%
  ungroup() %>%
  select(Name, Station, Obs, classe_dominante = xx)

litho_cameroun_2 <- intersection_cameroun %>%
  st_drop_geometry() %>%
  group_by(Name, Station, Obs, xx) %>%
  summarise(surface_totale = sum(surface_part), .groups = "drop") %>%
  group_by(Name, Station, Obs) %>%
  slice_max(surface_totale, n = 2, with_ties = FALSE) %>%
  slice(2) %>%              # garde uniquement la 2e ligne (2e rang) de chaque groupe
  ungroup() %>%
  select(Name, Station, Obs, classe_2 = xx)

litho_cameroun <- merge(litho_cameroun_1, litho_cameroun_2, by = c("Name","Station","Obs"), all.x = T)

litho_wp1 <- rbind(litho_france, litho_guadeloupe) %>% rbind(litho_cameroun)

write.csv(litho_wp1, "Lithologie/litho_wp1.csv")

```

# Land cover

## Corine Land Cover (France métropolitaine)

```{r get-corine}
clc <- st_read("Land Cover/Corine Land Cover/U2006_CLC2000_V2020_20u1.gpkg")
```


```{r clc-francemetro}
st_crs(bassins_france_proj)

clc_france_proj <- st_transform(clc, st_crs(bassins_france_proj))

bbox_bassins <- st_as_sfc(st_bbox(bassins_france_proj))
clc_reduit <- st_filter(clc_france_proj, bbox_bassins, .predicate = st_intersects)
clc_france <- st_intersection(clc_reduit, bassins_france_proj)

clc_france$surface <- st_area(clc_france)

landcover_francemetro <- clc_france |>
  st_drop_geometry() |>
  group_by(Name, Station, Obs, code_00) |>   # adaptez aux vrais noms de colonnes
  summarise(surface_totale = sum(Area_Ha)) |>
  group_by(Name,Station,Obs) |>
  mutate(part = surface_totale / sum(surface_totale))


clc_legend <- read_xlsx("Land Cover/Corine Land Cover/legend_clc.xlsx")

landcover_francemetro$clc_category <- clc_legend$clc_category[match(landcover_francemetro$code_00, clc_legend$Code)]

clc_wide_francemetro <- landcover_francemetro %>% select(-c("surface_totale", "code_00")) %>%
  group_by(Name,Station,Obs,clc_category) %>%
  summarize(part = sum(part)) %>% pivot_wider(names_from = clc_category, values_from = part)

clc_long_francemetro <- pivot_longer(clc_wide_francemetro, cols = unique(clc_legend$clc_category), names_to = "clc_category")

clc_long_francemetro$fullName <- with(clc_long_francemetro, paste(Station, Name, sep = " , "))


write.csv(clc_long_francemetro, "Land Cover/Corine Land Cover/clc_long_francemetro.csv")
write.csv(clc_wide_francemetro, "Land Cover/Corine Land Cover/clc_wide_francemetro.csv")
```
```{r plot-clc-francemetro}
clc_long_francemetro <- read.csv("Land Cover/Corine Land Cover/clc_long_francemetro.csv")
ggplot(clc_long_francemetro) + geom_col(aes(x=fullName, y=value, fill=clc_category))+
  scale_fill_manual(values= c("Agriculture" = "goldenrod2",
                             "Bare" = "peachpuff4", 
                             "Forest" = "springgreen4", 
                             "Glaciers and snow" = "lightcyan3",
                             "Grassland" = "darkolivegreen3",
                             "Inland water" = "steelblue",
                             "Marshes and bogs" = "navajowhite" ,
                             "Urban" = "grey"))+
  labs(x="",y="Part",fill="Catégorie")+
  theme_minimal()+
  theme(axis.text.x = element_text(angle = 45, h = 1 , v = 1))

```



## ESA WC

```{r fonction-gros-bassins}
extraction_esa <- function(poly_i, raster, degree = 0.5) {
  
  surface_km2 <- as.numeric(st_area(poly_i)) / 1e6
  
  if (surface_km2 < 5000) {
    r_crop <- crop(raster, poly_i)
    res <- exact_extract(r_crop, poly_i, fun = function(v, cf) tapply(cf, v, sum))
    return(tibble::enframe(res, name = "value", value = "coverage_fraction"))
  }
  
  # Découpage en grille de tuiles
  grille <- st_make_grid(poly_i, cellsize = degree)
  grille <- st_intersection(st_as_sf(grille), poly_i)
  grille <- st_collection_extract(grille, "POLYGON")
  grille <- grille[!st_is_empty(grille), ]
  grille <- grille[st_area(grille) > units::set_units(0, "m^2"), ]
  
  resultats_tuiles <- lapply(seq_len(nrow(grille)), function(j) {
    tuile_j <- grille[j, ]
    r_j <- crop(raster, tuile_j)
    exact_extract(r_j, tuile_j, fun = function(v, cf) tapply(cf, v, sum))
  })
  
  
  
  res <- Reduce(function(a, b) {
    codes <- union(names(a), names(b))
    setNames(sapply(codes, function(c) 
      (if (c %in% names(a)) a[[c]] else 0) + (if (c %in% names(b)) b[[c]] else 0)
    ), codes)
  }, resultats_tuiles)
  
  tibble::enframe(res, name = "value", value = "coverage_fraction")
}
```


### France metro

```{r extraction-esa-francemetro}
esawc_francemetro <- rast("Land Cover/World Cover ESA/esawc_francemetro.vrt")

bassins_france_esa <- st_transform(bassins_france_proj, crs(esawc_francemetro))


```

```{r boucle-esa-francemetro}

# Liste pour stocker uniquement les résumés (légers)
francemetro_wide <- vector("list", nrow(bassins_france_esa))

for (i in seq_len(nrow(bassins_france_esa))) {
  
  poly_i <- bassins_france_esa[i, ]
  
 francemetro_wide[[i]] <- tryCatch({
    
    # Découpe + extraction pixel par pixel
    r_crop <- crop(esawc_francemetro, poly_i)
    extrait_i <- extraction_esa(poly_i, r_crop)
    
    # Agrégation immédiate : surface totale par code d'occupation du sol
    resume_i <- extrait_i |>
      group_by(value) |>
      summarise(surface_totale = sum(coverage_fraction), .groups = "drop") |>
      mutate(
        part_pct = surface_totale / sum(surface_totale) * 100,
        Name = poly_i$Name,
        Station = poly_i$Station,
        Obs = poly_i$Obs
      ) |>
      select(Name, Station, Obs, value, part_pct) |>
      pivot_wider(names_from = value, values_from = part_pct, values_fill = 0)
    
    # Nettoyage mémoire avant de passer au bassin suivant
    rm(r_crop, extrait_i)
    gc()
    
    cat("Bassin", i, "/", nrow(bassins_france_esa), 
        "-", poly_i$Name, poly_i$Station, "OK\n")
    
    resume_i
    
  }, error = function(e) {
    cat("Erreur bassin", i, poly_i$Name, ":", conditionMessage(e), "\n")
    NULL
  })
}

# Fusion finale : une ligne par bassin, une colonne par code d'occupation du sol
esa_france_wide <- bind_rows(francemetro_wide)

write.csv(esa_france_wide, "Land Cover/World Cover ESA/esa_france_wide.csv")
```

```{r cinq-mars}

cinq_mars_esa <- bassins_france_esa[4,]
cinq_mars <-  vector("list", nrow(bassins_france_esa))


cinq_mars_esa <- st_make_valid(cinq_mars_esa)
  
grille <- st_make_grid(cinq_mars_esa, cellsize = 0.5)
grille <- st_intersection(st_as_sf(grille), cinq_mars_esa)
grille <- st_collection_extract(grille, "POLYGON")
grille <- grille[!st_is_empty(grille), ]
grille <- grille[st_area(grille) > units::set_units(0, "m^2"), ]
  
cinq_mars_tuiles <- lapply(seq_len(nrow(grille)), function(j) {
    tuile_j <- grille[j, ]
    r_j <- crop(esawc_francemetro, tuile_j)
    exact_extract(r_j, tuile_j, fun = function(v, cf) tapply(cf, v, sum))
  })
    
    

extrait_cinq_mars <- lapply(cinq_mars_tuiles, function(i){i %>% as.data.frame %>% tibble::rownames_to_column() %>% setNames(c("value","coverage_fraction"))}) %>%
        bind_rows() %>% group_by(value) %>% summarise(coverage_fraction = sum(coverage_fraction))


    
# Agrégation immédiate : surface totale par code d'occupation du sol
    resume_cinq_mars <- extrait_cinq_mars |>
      group_by(value) |>
      summarise(surface_totale = sum(coverage_fraction), .groups = "drop") |>
      mutate(
        part_pct = surface_totale / sum(surface_totale) * 100,
        Name = cinq_mars_esa$Name,
        Station = cinq_mars_esa$Station,
        Obs = cinq_mars_esa$Obs
      ) |>
      select(Name, Station, Obs, value, part_pct) |>
      pivot_wider(names_from = value, values_from = part_pct, values_fill = 0)
    
write.csv(resume_cinq_mars, "Land Cover/World Cover ESA/cinq_mars_wide.csv")

```


```{r temporaire-gros-10-11}

gros_BV_esa <- bassins_france_esa[c(10,11),]
gros_BV <-  vector("list", nrow(bassins_france_esa))


for (i in c(10,11)) {
  
  poly_i <- bassins_france_esa[i, ]
  poly_i <- st_make_valid(poly_i)
  
 gros_BV[[i]] <- tryCatch({
    
    # Découpe + extraction pixel par pixel
    r_crop <- crop(esawc_francemetro, poly_i)
    
        r_crop <- crop(esawc_francemetro, poly_i)
    
     grille <- st_make_grid(poly_i, cellsize = 0.5)
  grille <- st_intersection(st_as_sf(grille), poly_i)
  grille <- st_collection_extract(grille, "POLYGON")
  grille <- grille[!st_is_empty(grille), ]
  grille <- grille[st_area(grille) > units::set_units(0, "m^2"), ]
  
resultats_tuiles <- lapply(seq_len(nrow(grille)), function(j) {
    tuile_j <- grille[j, ]
    r_j <- crop(esawc_francemetro, tuile_j)
    exact_extract(r_j, tuile_j, fun = function(v, cf) tapply(cf, v, sum))
  })
    
    

  extrait_i <- lapply(resultats_tuiles, function(i){i %>% as.data.frame %>%
        tibble::rownames_to_column() %>% setNames(c("value","coverage_fraction"))}) %>%
        bind_rows() %>% group_by(value) %>% summarise(coverage_fraction = sum(coverage_fraction))
    
    # Agrégation immédiate : surface totale par code d'occupation du sol
    resume_i <- extrait_i |>
      group_by(value) |>
      summarise(surface_totale = sum(coverage_fraction), .groups = "drop") |>
      mutate(
        part_pct = surface_totale / sum(surface_totale) * 100,
        Name = poly_i$Name,
        Station = poly_i$Station,
        Obs = poly_i$Obs
      ) |>
      select(Name, Station, Obs, value, part_pct) |>
      pivot_wider(names_from = value, values_from = part_pct, values_fill = 0)
    
    # Nettoyage mémoire avant de passer au bassin suivant
    rm(r_crop, extrait_i)
    gc()
    
    cat("Bassin", i, "/", nrow(bassins_france_esa), 
        "-", poly_i$Name, poly_i$Station, "OK\n")
    
    resume_i
    
  }, error = function(e) {
    cat("Erreur bassin", i, poly_i$Name, ":", conditionMessage(e), "\n")
    NULL
  })
}

# Fusion finale : une ligne par bassin, une colonne par code d'occupation du sol
gros_BV_10_11_wide <- bind_rows(gros_BV)
write.csv(gros_BV_10_11_wide, "Land Cover/World Cover ESA/esa_france.10.11_wide.csv")
```


```{r temporaire-loire-montjean}


montjean_BV <-  vector("list", nrow(montjean_esa))

montjean_esa <- bassins_france_esa[15,]
montjean_esa <- st_make_valid(montjean_esa)
montjean_esa <- st_buffer(montjean_esa, 0)
  
# Découpe + extraction pixel par pixel
r_crop <- crop(esawc_francemetro, montjean_esa)

  grille <- st_make_grid(montjean_esa, cellsize = 0.5)
  grille <- st_intersection(st_as_sf(grille), montjean_esa)
  grille <- st_collection_extract(grille, "POLYGON")
  grille <- grille[!st_is_empty(grille), ]
  grille <- grille[st_area(grille) > units::set_units(0, "m^2"), ]
  
resultats_tuiles <- lapply(seq_len(nrow(grille)), function(j) {
    tuile_j <- grille[j, ]
    r_j <- crop(esawc_francemetro, tuile_j)
    exact_extract(r_j, tuile_j, fun = function(v, cf) tapply(cf, v, sum))
  })
    
    
extrait_montjean <- lapply(resultats_tuiles, function(i){i %>% as.data.frame %>% tibble::rownames_to_column() %>% setNames(c("value","coverage_fraction"))}) %>%
        bind_rows() %>% group_by(value) %>% summarise(coverage_fraction = sum(coverage_fraction))


# Agrégation immédiate : surface totale par code d'occupation du sol
resume_montjean <- extrait_montjean |>
      group_by(value) |>
      summarise(surface_totale = sum(coverage_fraction), .groups = "drop") |>
      mutate(
        part_pct = surface_totale / sum(surface_totale) * 100,
        Name = montjean_esa$Name,
        Station = montjean_esa$Station,
        Obs = montjean_esa$Obs
      ) |>
      select(Name, Station, Obs, value, part_pct) |>
      pivot_wider(names_from = value, values_from = part_pct, values_fill = 0)
    
```

```{r temporaire-loire-stsatur}
stsatur_BV <-  vector("list", 1)

stsatur_esa <- bassins_france_esa[20,]
stsatur_esa <- st_make_valid(stsatur_esa)
stsatur_esa <- st_buffer(stsatur_esa, 0)
  
# Découpe + extraction pixel par pixel
r_crop <- crop(esawc_francemetro, stsatur_esa)

  grille <- st_make_grid(stsatur_esa, cellsize = 0.5)
  grille <- st_intersection(st_as_sf(grille), stsatur_esa)
  grille <- st_collection_extract(grille, "POLYGON")
  grille <- st_make_valid(grille)
  grille <- grille[!st_is_empty(grille), ]
  grille <- grille[st_area(grille) > units::set_units(0, "m^2"), ]
  
resultats_tuiles <- lapply(seq_len(nrow(grille)), function(j) {
    tuile_j <- grille[j, ]
    r_j <- crop(esawc_francemetro, tuile_j)
    exact_extract(r_j, tuile_j, fun = function(v, cf) tapply(cf, v, sum))
  })
    
    
extrait_stsatur <- lapply(resultats_tuiles, function(i){i %>% as.data.frame %>% tibble::rownames_to_column() %>% setNames(c("value","coverage_fraction"))}) %>%
        bind_rows() %>% group_by(value) %>% summarise(coverage_fraction = sum(coverage_fraction))


# Agrégation immédiate : surface totale par code d'occupation du sol
resume_stsatur <- extrait_stsatur |>
      group_by(value) |>
      summarise(surface_totale = sum(coverage_fraction), .groups = "drop") |>
      mutate(
        part_pct = surface_totale / sum(surface_totale) * 100,
        Name = stsatur_esa$Name,
        Station = stsatur_esa$Station,
        Obs = stsatur_esa$Obs
      ) |>
      select(Name, Station, Obs, value, part_pct) |>
      pivot_wider(names_from = value, values_from = part_pct, values_fill = 0)
    
BV_15_20 <- rbind(resume_montjean,resume_stsatur)
write.csv(BV_15_20, "Land Cover/World Cover ESA/esa_france.15.20_wide.csv")
```


### Guadeloupe

```{r extraction-esa-guadeloupe}
esawc_guadeloupe <- rast("Land Cover/World Cover ESA/World Cover ESA.vrt")

bassins_guadeloupe_esa <- st_transform(bassins_guadeloupe_proj, crs(esawc_guadeloupe))

```


```{r boucle-esa-guadeloupe}

# Liste pour stocker uniquement les résumés (légers)
guadeloupe_wide <- vector("list", nrow(bassins_guadeloupe_esa))

for (i in seq_len(nrow(bassins_guadeloupe_esa))) {
  
  poly_i <- bassins_guadeloupe_esa[i, ]
  
 guadeloupe_wide[[i]] <- tryCatch({
    
    # Découpe + extraction pixel par pixel
    r_crop <- crop(esawc_guadeloupe, poly_i)
    extrait_i <- extraction_esa(poly_i, r_crop) %>% as.data.frame %>% tibble::rownames_to_column() %>% setNames(c("value","coverage_fraction"))
    
    # Agrégation immédiate : surface totale par code d'occupation du sol
    resume_i <- extrait_i |>
      group_by(value) |>
      summarise(surface_totale = sum(coverage_fraction), .groups = "drop") |>
      mutate(
        part_pct = surface_totale / sum(surface_totale) * 100,
        Name = poly_i$Name,
        Station = poly_i$Station,
        Obs = poly_i$Obs
      ) |>
      select(Name, Station, Obs, value, part_pct) |>
      pivot_wider(names_from = value, values_from = part_pct, values_fill = 0)
    
    # Nettoyage mémoire avant de passer au bassin suivant
    rm(r_crop, extrait_i)
    gc()
    
    cat("Bassin", i, "/", nrow(bassins_guadeloupe_esa), 
        "-", poly_i$Name, poly_i$Station, "OK\n")
    
    resume_i
    
  }, error = function(e) {
    cat("Erreur bassin", i, poly_i$Name, ":", conditionMessage(e), "\n")
    NULL
  })
}

# Fusion finale : une ligne par bassin, une colonne par code d'occupation du sol
esa_guadeloupe_wide <- bind_rows(guadeloupe_wide)

write.csv(esa_guadeloupe_wide, "Land Cover/World Cover ESA/esa_guadeloupe_wide.csv")
```

### Cameroun

```{r extraction-esa-cameroun}
esawc_cameroun <- rast("Land Cover/World Cover ESA/esawc_cameroun.vrt")

bassins_cameroun_esa <- st_transform(bassins_hors_france, crs(esawc_cameroun))

```


```{r esa-cameroun}

# Liste pour stocker uniquement les résumés (légers)
cameroun_wide <- vector("list", nrow(bassins_cameroun_esa))

# Messam
poly_messam <- bassins_cameroun_esa[2, ]
extrait_messam <- extraction_esa(poly_messam, esawc_cameroun) %>% as.data.frame %>% tibble::rownames_to_column() %>% setNames(c("value","coverage_fraction"))
    
resume_messam <- extrait_messam |>
      group_by(value) |>
      summarise(surface_totale = sum(coverage_fraction), .groups = "drop") |>
      mutate(
        part_pct = surface_totale / sum(surface_totale) * 100,
        Name = poly_i$Name,
        Station = poly_i$Station,
        Obs = poly_i$Obs
      ) |>
      select(Name, Station, Obs, value, part_pct) |>
      pivot_wider(names_from = value, values_from = part_pct, values_fill = 0)


# Olama
poly_olama <- bassins_cameroun_esa[3, ]
  # Sinon, découpage en grille de tuiles
grille <- st_make_grid(poly_olama, cellsize = 0.5)
grille <- st_intersection(st_as_sf(grille), poly_olama)
# Extraire uniquement la composante polygonale de chaque géométrie
grille <- st_collection_extract(grille, "POLYGON")
# Retirer les géométries vides ou invalides résiduelles
grille <- grille[!st_is_empty(grille), ]
grille <- grille[st_area(grille) > units::set_units(0, "m^2"), ]  # retirer tuiles vides
  
olama_tuiles <- lapply(seq_len(nrow(grille)), function(j) {
    tuile_j <- grille[j, ]
    r_j <- crop(esawc_cameroun, tuile_j)
    exact_extract(r_j, tuile_j, fun = function(v, cf) tapply(cf, v, sum))
  })
  
  
extrait_olama <- lapply(olama_tuiles, function(i){i %>% as.data.frame %>% tibble::rownames_to_column() %>% setNames(c("value","coverage_fraction"))}) %>%
        bind_rows() %>% group_by(value) %>% summarise(coverage_fraction = sum(coverage_fraction))

resume_olama <- extrait_olama |>
      group_by(value) |>
      summarise(surface_totale = sum(coverage_fraction), .groups = "drop") |>
      mutate(
        part_pct = surface_totale / sum(surface_totale) * 100,
        Name = poly_i$Name,
        Station = poly_i$Station,
        Obs = poly_i$Obs
      ) |>
      select(Name, Station, Obs, value, part_pct) |>
      pivot_wider(names_from = value, values_from = part_pct, values_fill = 0)
    
# Mbalmayo
poly_mbalmayo <- bassins_cameroun_esa[3, ]
  # Sinon, découpage en grille de tuiles
grille <- st_make_grid(poly_mbalmayo, cellsize = 0.5)
grille <- st_intersection(st_as_sf(grille), poly_mbalmayo)
# Extraire uniquement la composante polygonale de chaque géométrie
grille <- st_collection_extract(grille, "POLYGON")
# Retirer les géométries vides ou invalides résiduelles
grille <- grille[!st_is_empty(grille), ]
grille <- grille[st_area(grille) > units::set_units(0, "m^2"), ]  # retirer tuiles vides
  
mbalmayo_tuiles <- lapply(seq_len(nrow(grille)), function(j) {
    tuile_j <- grille[j, ]
    r_j <- crop(esawc_cameroun, tuile_j)
    exact_extract(r_j, tuile_j, fun = function(v, cf) tapply(cf, v, sum))
  })
  
  
extrait_mbalmayo <- lapply(mbalmayo_tuiles, function(i){i %>% as.data.frame %>% tibble::rownames_to_column() %>% setNames(c("value","coverage_fraction"))}) %>%
        bind_rows() %>% group_by(value) %>% summarise(coverage_fraction = sum(coverage_fraction))

resume_mbalmayo <- extrait_mbalmayo |>
      group_by(value) |>
      summarise(surface_totale = sum(coverage_fraction), .groups = "drop") |>
      mutate(
        part_pct = surface_totale / sum(surface_totale) * 100,
        Name = poly_i$Name,
        Station = poly_i$Station,
        Obs = poly_i$Obs
      ) |>
      select(Name, Station, Obs, value, part_pct) |>
      pivot_wider(names_from = value, values_from = part_pct, values_fill = 0)


names(resume_mbalmayo)
names(resume_messam)
names(resume_olama)


resume_messam$`40` <- NA
resume_messam$`60` <- NA

esa_cameroun_wide <- rbind(resume_mbalmayo,resume_messam) %>% rbind(resume_olama)

write.csv(esa_cameroun_wide, "Land Cover/World Cover ESA/esa_cameroun_wide.csv")
```

### All

```{r join-df}
esa_france <-   read.csv("Land Cover/World Cover ESA/esa_france_wide.csv")
dim(esa_france)
esa_cinqmars <-   read.csv("Land Cover/World Cover ESA/cinq_mars_wide.csv")
dim(esa_cinqmars)
esa_france_10.11 <- read.csv("Land Cover/World Cover ESA/esa_france.10.11_wide.csv")
dim(esa_france_10.11)
esa_france_15.20 <- read.csv("Land Cover/World Cover ESA/esa_france.15.20_wide.csv")
dim(esa_france_15.20)

setdiff(names(esa_france), names(esa_cinqmars))
esa_cinqmars$X100 <- NA
esa_cinqmars$X70 <- NA

setdiff(names(esa_france), names(esa_france_15.20))
esa_france_15.20$X100 <- NA
esa_france_15.20$X70 <- NA

esa_guadeloupe <- read.csv("Land Cover/World Cover ESA/esa_guadeloupe_wide.csv")
dim(esa_guadeloupe)

setdiff(names(esa_france), names(esa_guadeloupe))
esa_guadeloupe <- mutate(esa_guadeloupe, X40 = NA, X50 = NA, X80 = NA, X90 = NA, X100 = NA, X70 = NA)


esa_cameroun <- read.csv("Land Cover/World Cover ESA/esa_cameroun_wide.csv")
dim(esa_cameroun)

setdiff(names(esa_france), names(esa_cameroun))
esa_cameroun <- mutate(esa_cameroun, X100 = NA, X70 = NA)


esa_wp1 <- rbind(esa_france, esa_cinqmars) %>% rbind(esa_france_10.11) %>% rbind(esa_france_15.20) %>% rbind(esa_guadeloupe) %>% rbind(esa_cameroun) %>% mutate(X = NULL)

write.csv(esa_wp1, "Land Cover/World Cover ESA/esa_wp1.csv")
  
esawc_classes <- c("10" = "Tree Cover",
                   "20" = "Shrubland",
                   "30" = "Grassland",
                   "40" = "Cropland",
                   "50" = "Built up",
                   "60" = "Bare",
                   "70" = "Snow and ice",
                   "80" = "Permanent water bodies",
                   "90" = "Herbaceous wetland",
                   "10" = "Mangroves",
                   "11" = "Moss and lichen")


```

# Merge all characteristics

```{r read-merge-df}
precip_wp1 <- read.csv("ERA5/bassins_precip.csv")
temp_wp1 <- read.csv("ERA5/bassins_temp.csv")
dem_wp1 <- read.csv("DEM/dem_wp1.csv")
litho_wp1 <- read.csv("Lithologie/litho_wp1.csv")
pedo_wp1 <- read.csv("Pédologie/resultats_sols.csv")*
clc_wp1 <- read.cdv("Land Cover/Corine Land Cover/clc_wide_francemetro.csv")
easwc_wp1 <- read.csv("Land Cover/World Cover ESA/esa_wp1.csv")
```

